version: 0.2

phases:

  build:
    commands:
      - docker build -t goapp:latest .

  post_build:
    run-as: root 
    commands:
  
      - docker tag goapp:latest danyalharoon/goapp:latest
      - docker push danyalharoon:goapp:latest 

#artifacts:
 # files: myapp
