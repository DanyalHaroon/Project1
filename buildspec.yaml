version: 0.2

phases:

  build:
    commands:
      - docker build -t myapp .

  post_build:
    run-as: root 
    commands:
  
      - docker login --username=danyalharoon --password=~4##&qr@>zX2baj
      - docker tag myapp:latest danyalharoon/goapp:latest
      - docker push danyalharoon/goapp

#artifacts:
 # files: myapp
